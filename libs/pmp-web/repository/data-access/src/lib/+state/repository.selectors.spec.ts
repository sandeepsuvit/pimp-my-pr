import { RepositoryState } from './repository.reducer';
import { repositoryQuery } from './repository.selectors';

describe('Repository Selectors', () => {
  const ERROR_MSG = 'No Error Available';
  const getRepositoryId = it => it['id'];

  beforeEach(() => {});
});
